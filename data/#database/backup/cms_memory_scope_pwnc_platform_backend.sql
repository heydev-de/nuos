CREATE TABLE `cms_memory_scope_pwnc_platform_backend` (
  `id` int unsigned NOT NULL AUTO_INCREMENT,
  `mnemonic` varchar(500) COLLATE utf8mb4_unicode_ci NOT NULL,
  `description` varchar(500) COLLATE utf8mb4_unicode_ci NOT NULL,
  `content` text COLLATE utf8mb4_unicode_ci NOT NULL,
  `attachment_type` varchar(100) COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT '',
  `owner` varchar(40) COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT '',
  `created` datetime NOT NULL DEFAULT CURRENT_TIMESTAMP,
  `editor` varchar(40) COLLATE utf8mb4_unicode_ci NOT NULL DEFAULT '',
  `edited` datetime DEFAULT NULL,
  `access_count` int unsigned NOT NULL DEFAULT '0',
  `accessed` datetime DEFAULT NULL,
  `followup` datetime DEFAULT NULL,
  `read_only` tinyint(1) NOT NULL DEFAULT '0',
  `safe` tinyint(1) NOT NULL DEFAULT '0',
  `rating_value` decimal(3,2) unsigned NOT NULL DEFAULT '3.00',
  `rating_sum` int unsigned NOT NULL DEFAULT '0',
  `rating_count` int unsigned NOT NULL DEFAULT '0',
  PRIMARY KEY (`id`),
  KEY `owner` (`owner`),
  FULLTEXT KEY `mnemonic` (`mnemonic`),
  FULLTEXT KEY `description` (`description`),
  FULLTEXT KEY `content` (`content`),
  FULLTEXT KEY `mnemonic_2` (`mnemonic`,`description`,`content`) 
) ENGINE=InnoDB AUTO_INCREMENT=3 DEFAULT CHARSET=utf8mb4 COLLATE=utf8mb4_unicode_ci